## 推荐项目题目

# **RepoMind-RL：面向真实代码仓库的记忆驱动自进化 Debug Agent Harness**

介绍：

> RepoMind-RL 是一个能在真实代码仓库中自动定位 Bug、生成补丁、运行测试并沉淀经验的 Coding Agent。它通过独创的“奖励门控因果记忆策略”管理长期经验，并用 Agentic RL 学习何时检索、写入、更新、遗忘记忆，从而让 Agent 在多轮任务中越修越聪明。

为什么这个方向有含金量：现在 Agentic RL 的核心趋势，是把 LLM 从单轮文本生成器变成在动态环境中持续感知、规划、行动、反思的决策智能体；相关综述也把 planning、tool use、memory、self-improvement 等列为核心能力。([arXiv](https://arxiv.org/abs/2509.02547?utm_source=chatgpt.com "The Landscape of Agentic Reinforcement Learning for LLMs: A Survey")) 同时，Memory-R1 和 AgeMem 这类工作已经证明，“让 Agent 用 RL 学会管理记忆”比固定 RAG/启发式记忆更有研究价值。([arXiv](https://arxiv.org/abs/2508.19828?utm_source=chatgpt.com "Memory-R1: Enhancing Large Language Model Agents to Manage and Utilize Memories via Reinforcement Learning")) OpenAI 最近讲的 Harness Engineering 也强调，工程师的角色正在从“亲自写代码”转向“设计环境、指定意图、构建反馈闭环”，这正好可以成为你的架构亮点。([OpenAI](https://openai.com/index/harness-engineering/?utm_source=chatgpt.com "Harness engineering: leveraging Codex in an agent-first world - OpenAI"))

---

## 项目最终效果

用户输入一个真实 issue，例如：

> “这个 FastAPI 项目的订单状态偶尔不会从 pending 更新到 paid，请定位并修复。”

Agent 会自动完成：

1. 解析 issue，生成调试计划。
    
2. 检索仓库结构、历史修复经验、相似 Bug 记忆。
    
3. 调用工具：搜索代码、查看调用链、运行测试、插桩、执行 debugger。
    
4. 生成 patch。
    
5. 运行测试验证。
    
6. 如果失败，反思并重试。
    
7. 把这次任务沉淀成可复用记忆。
    
8. 用 RL 更新“以后什么时候该查记忆、写记忆、删记忆、总结记忆”。
    

你最后可以展示一个 Demo Dashboard：

- 当前 Agent 轨迹：Plan → Search → Test → Patch → Verify。
    
- 使用了哪些记忆。
    
- 哪些记忆帮助成功，哪些误导失败。
    
- RL 策略更新后，Agent 的成功率、平均 token、平均工具调用次数是否下降。
    
- 对比：无记忆 / 普通 RAG 记忆 / 你的 RL 记忆策略。
    

---

## 独创点：Reward-Gated Causal Memory，奖励门控因果记忆

普通 Agent 记忆通常是：

> 做完任务 → 总结 → 存进向量库 → 下次相似检索。

这个很常见，面试官不会觉得新。

你的记忆策略可以设计成：

## **RGCM：Reward-Gated Causal Memory**

每条记忆不是简单文本，而是一张结构化 Memory Card：

```json
{
  "memory_id": "...",
  "type": "episodic | semantic | procedural | anti_pattern",
  "scope": "repo/module/file/function",
  "trigger": "什么任务条件下应该使用它",
  "content": "具体经验",
  "evidence": ["测试通过记录", "相关文件路径", "trace id"],
  "reward_credit": 0.73,
  "reuse_success": 5,
  "reuse_failure": 1,
  "conflict_score": 0.12,
  "last_used_at": "...",
  "status": "draft | verified | deprecated"
}
```

记忆分四类：

**1. Episodic Memory，情节记忆**  
记录一次完整任务轨迹：issue、搜索路径、失败尝试、最终 patch、测试结果。

**2. Semantic Memory，语义记忆**  
记录仓库知识：模块边界、核心类职责、数据库表含义、API contract。

**3. Procedural Memory，程序性记忆**  
记录可复用修复套路，例如：

> “FastAPI 中 payment webhook 修复时，优先检查 idempotency key、事务提交、异步任务重试。”

**4. Anti-pattern Memory，反模式记忆**  
这是你的亮点之一。失败轨迹不直接丢弃，而是变成“不要这样做”的负向经验：

> “当测试失败原因为并发状态竞争时，不要只改接口返回值，应该检查事务边界和锁。”

记忆更新公式可以简单实现为：

```text
memory_score =
  0.35 * reward_credit
+ 0.25 * reuse_success_rate
+ 0.15 * evidence_strength
+ 0.10 * recency
+ 0.10 * scope_match
- 0.20 * conflict_score
- 0.15 * stale_penalty
```

核心创新在于：

> 只有被测试、日志、静态分析或最终 reward 证明有效的记忆，才能从 draft 晋升为 verified。误导 Agent 的记忆不会立刻删除，而是转化成 anti-pattern 或降低权重。

这比普通“向量库长期记忆”更有解释性，也更像生产系统。

---

## Agentic RL 怎么做，既有技术含量又能 2 个月落地

不要一开始就训练大模型本体，那样 2 个月风险太高。建议你把 RL 用在 **Agent Harness Policy** 上。

也就是说：

> LLM 负责推理和生成，RL Policy 负责决定关键动作：什么时候查记忆、查哪类记忆、要不要写入、是否总结、是否删除、是否继续调试、是否提交 patch。

这很符合 Agent Lightning 这类方向：把 Agent 执行和 RL 训练解耦，把轨迹转成 transition，再做 credit assignment。([arXiv](https://arxiv.org/abs/2508.03680?utm_source=chatgpt.com "Agent Lightning: Train ANY AI Agents with Reinforcement Learning"))

### RL 形式

你可以把任务建模成 POMDP：

```text
state:
  当前 issue 表征
  当前代码上下文
  最近工具调用结果
  测试状态
  已检索记忆数量
  记忆置信度
  当前 token/cost budget
  失败次数

action:
  RETRIEVE_EPISODIC
  RETRIEVE_SEMANTIC
  RETRIEVE_PROCEDURAL
  WRITE_MEMORY
  UPDATE_MEMORY
  DELETE_OR_DEPRECATE_MEMORY
  SUMMARIZE_CONTEXT
  RUN_TEST
  SEARCH_CODE
  PATCH
  STOP_AND_SUBMIT

reward:
  +1.0  测试全部通过
  +0.5  fail-to-pass 测试通过
  +0.2  定位到正确文件
  +0.1  patch 较小且无无关改动
  -0.1  工具调用过多
  -0.1  token 成本过高
  -0.3  使用了被验证为误导的记忆
  -1.0  patch 破坏原有测试
```

### 训练难度分三档

**基础版，必须完成：Contextual Bandit / DQN 训练记忆选择器**  
输入 state，输出 memory action。这个两个月内最稳。

**进阶版，加分项：PPO 训练工具/记忆控制器**  
不训练 LLM，只训练一个小 policy network。

**冲刺版，论文味最浓：LoRA + GRPO 训练小模型做 memory manager**  
用 Qwen2.5-Coder-1.5B / 3B 之类模型训练一个专门的 memory manager。这个可以作为 stretch goal，不作为主线承诺。

---

## Harness Engineering 架构设计

项目可以分成 6 层：

```text
┌────────────────────────────────────────────┐
│  Dashboard / Eval Report                   │
│  轨迹可视化、记忆贡献、RL reward 曲线         │
├────────────────────────────────────────────┤
│  Evaluation Harness                        │
│  SWE-bench subset / debug-gym / 自建 bug集   │
├────────────────────────────────────────────┤
│  Agent Runtime                             │
│  Planner → Executor → Verifier → Reflector │
├────────────────────────────────────────────┤
│  RL Policy Layer                           │
│  Memory action policy / Tool action policy │
├────────────────────────────────────────────┤
│  Memory Layer                              │
│  STM + LTM + Memory Graph + Anti-patterns  │
├────────────────────────────────────────────┤
│  Tool Sandbox                              │
│  grep、AST、pytest、pdb、git diff、Docker    │
└────────────────────────────────────────────┘
```

这层架构非常适合面试讲。

你不是在说“我调了几个 prompt”，而是在说：

> 我设计了一个可观测、可评测、可训练、可回放的 Agent Harness。

这和 OpenAI 对 Agent 构建的实践建议一致：Agent 系统通常由模型、工具、指令/护栏、编排和评测组成，而不是只有一个聊天入口。([OpenAI](https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/?utm_source=chatgpt.com "A practical guide to building agents - OpenAI"))

---

## 评测任务怎么选

建议你用三类任务，难度逐步增加。

### 第一类：自建 Backend Bug Bench

你有后端基础，可以自己构造 30～50 个小型真实 Bug：

- FastAPI / Spring Boot / Django 项目。
    
- 数据库事务 Bug。
    
- 缓存一致性 Bug。
    
- 权限校验 Bug。
    
- 并发更新 Bug。
    
- 参数边界 Bug。
    
- 异步任务重试 Bug。
    

优点：可控、容易跑、适合展示。

### 第二类：debug-gym

debug-gym 是一个面向交互式调试的文本环境，提供 pdb、代码导航、变量打印、测试函数创建等工具，适合训练和评测 Coding Agent 的主动探索能力。([arXiv](https://arxiv.org/abs/2503.21557?utm_source=chatgpt.com "debug-gym: A Text-Based Environment for Interactive Debugging"))

### 第三类：SWE-bench Lite / Verified 子集

SWE-bench 用真实 GitHub issue 评测模型生成 patch 的能力；每个样本来自已解决的 GitHub issue，并配有 PR 和测试来验证修复正确性。([GitHub](https://github.com/swe-bench/SWE-bench?utm_source=chatgpt.com "GitHub - SWE-bench/SWE-bench: SWE-bench: Can Language Models Resolve ...")) 你不需要全量跑，选 20～50 个 Python issue 就够做毕业级/秋招级展示。

---

## 两个月排期

### 第 1 周：确定任务和基线

目标：

- 搭好 FastAPI 后端 + Docker sandbox。
    
- 支持输入 issue，Agent 能搜索代码、读文件、跑测试。
    
- 接入 LLM API。
    
- 保存完整 trajectory 日志。
    

产出：

- 一个最小 Coding Agent。
    
- 一份 baseline 报告：无记忆、无 RL 的成功率。
    

---

### 第 2 周：Agent Runtime + Tool Harness（使用 LangGraph）

实现工具：

- `search_code`
    
- `read_file`
    
- `run_pytest`
    
- `run_debugger`
    
- `edit_file`
    
- `git_diff`
    
- `static_check`
    
- `submit_patch`
    

重点不是工具数量，而是每个工具都有：

- 输入 schema。
    
- 输出 schema。
    
- 错误处理。
    
- trace id。
    
- 可回放日志。
    

这就是 harness engineering 的味道。

---

### 第 3 周：记忆层 v1

实现：

- 短期记忆 STM：当前任务 scratchpad。
    
- 长期记忆 LTM：SQLite/Postgres + 向量库。
    
- Memory Card schema。
    
- 记忆检索：scope match + vector search + keyword search。
    
- 任务结束后自动写入 episodic memory。
    

先做普通记忆基线。

---

### 第 4 周：RGCM 独创记忆策略

实现：

- reward-gated promotion：记忆分 draft / verified / deprecated。
    
- anti-pattern memory：失败轨迹转负向经验。
    
- memory score 更新。
    
- conflict detection：新旧记忆冲突时降权。
    
- procedural consolidation：相似成功经验超过 N 次，合并成 playbook。
    

这周是项目差异化核心。

---

### 第 5 周：RL Policy v1

实现一个轻量 RL controller：

- 输入 state feature。
    
- 输出 memory/tool action。
    
- 先用 contextual bandit 或 DQN。
    
- reward 来自测试结果、patch diff、工具成本、记忆贡献。
    

你可以不用训练大模型，只训练 policy network。面试时可以说：

> 我把 LLM 放在推理层，把 RL 放在 Harness 决策层，这样训练成本低、可解释、可控，也更适合工程落地。

---

### 第 6 周：评测与消融实验

做四组对比：

| 方法                | 说明       |
| ----------------- | -------- |
| No Memory Agent   | 无长期记忆    |
| Static RAG Memory | 普通向量检索   |
| RGCM Memory       | 你的奖励门控记忆 |
| RGCM + RL Policy  | 完整方法     |

指标：

- 修复成功率。
    
- 平均工具调用次数。
    
- 平均 token 消耗。
    
- 平均修复时间。
    
- patch 大小。
    
- 记忆命中率。
    
- 记忆误导率。
    
- 重复 Bug 上的性能提升。
    

---

### 第 7 周：Dashboard + 工程包装

做一个漂亮展示：

- 任务列表。
    
- 每个任务的 Agent trajectory。
    
- 当前调用了哪些工具。
    
- 检索了哪些记忆。
    
- 记忆贡献分。
    
- reward 曲线。
    
- 对比实验柱状图。
    
- patch diff 展示。
    

后端实习背景在这里很加分：你可以把它做成一个完整系统，而不是 notebook。

---

### 第 8 周：论文式 README + 简历材料

准备：

- 项目 README。
    
- 架构图。
    
- 设计文档。
    
- 实验报告。
    
- Demo 视频。
    
- 简历 bullets。
    
- 面试 Q&A。
    

README 标题可以写：

> RepoMind-RL: A Reward-Gated Memory Harness for Self-Improving Coding Agents

---

## 技术栈建议

后端：

- Python + FastAPI
    
- PostgreSQL / SQLite
    
- Redis / Celery，可选
    
- Docker sandbox
    
- SQLAlchemy
    
- Pydantic
    

Agent：

- LangGraph 作为参考，但核心 harness 自己实现
    

记忆：

- SQLite/Postgres 存 Memory Card
    
- Chroma / Qdrant / FAISS 做向量检索
    
- NetworkX 做 memory graph
    
- BM25 + embedding hybrid search
    

RL：

- PyTorch
    
- stable-baselines3 或自己写 DQN / PPO
    
- 可选 trl / verl / OpenRLHF 做小模型 GRPO
    

评测：

- pytest
    
- Docker
    
- debug-gym
    
- SWE-bench subset
    

前端：

- React + Tailwind
    
- ECharts / Recharts
    
- Monaco diff viewer
    

---

## 简历上可以这么写

你可以写成这样：

> **RepoMind-RL：面向真实代码仓库的自进化 Debug Agent Harness**  
> 构建了一个可回放、可评测、可训练的 Coding Agent 系统，支持代码搜索、测试执行、补丁生成、调试工具调用和轨迹追踪。设计 Reward-Gated Causal Memory 策略，将任务轨迹沉淀为 episodic / semantic / procedural / anti-pattern 四类记忆，并基于测试 reward、复用成功率和冲突分数动态更新记忆权重。进一步将 Agent 执行过程建模为 POMDP，使用 DQN/PPO 训练 memory-tool policy，使 Agent 学习何时检索、写入、更新或遗忘记忆。在自建 Backend Bug Bench 和 SWE-bench 子集上，相比普通 RAG Memory baseline 提升修复成功率并降低平均工具调用成本。

---

## 面试时最能打的 5 个点

### 1. 你不是做 Chatbot，而是做 Agent Harness

可以讲：

> 我认为 Agent 的难点不只是 prompt，而是环境、工具、反馈、评测和可观测性。所以我把系统设计成 Harness，所有工具调用、测试反馈、记忆更新、reward 都可追踪、可回放、可训练。

### 2. 你的记忆不是普通 RAG

可以讲：

> 普通 RAG 只解决“能不能查到”，但不解决“查到的经验是否可靠、是否过时、是否曾经误导过 Agent”。所以我设计了 reward-gated memory，把记忆和任务结果绑定。

### 3. 你的 RL 很务实

可以讲：

> 我没有盲目训练大模型，而是训练 Harness Policy。LLM 负责语义推理，RL 负责策略决策，这样成本低、收敛快、可解释，也更容易工程落地。

### 4. 你有消融实验

面试官会很喜欢这个：

> 我做了 No Memory、Static RAG、RGCM、RGCM + RL 四组对比，证明提升不是 prompt 偶然带来的。

---

## 最小可行版本范围

两个月内建议你控制目标：

- 自建 30 个 backend bug task。
    
- 跑 10～20 个 SWE-bench/debug-gym task。
    
- RL 先训练 memory action policy，不训练大模型。
    
- Dashboard 做到能展示 3～5 条完整成功轨迹。
    
- 实验结果不追求 SOTA，追求设计完整、可解释、有对比。
    

最终你的项目定位可以是：

> 一个研究型工程项目，不是单纯应用项目；一个 Agent 系统，不是 LLM wrapper；一个可训练 Harness，不是 prompt demo。